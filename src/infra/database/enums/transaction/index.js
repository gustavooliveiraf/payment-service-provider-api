const paymentMethod = require('./paymentMethod');
const captureMethod = require('./captureMethod');
const status = require('./status');

module.exports = {
  paymentMethod,
  captureMethod,
  status,
};
